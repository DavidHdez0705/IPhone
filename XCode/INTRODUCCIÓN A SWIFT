			INTRODUCCIÓN A SWIFT

var myVariable- variables 
let myConstant-  constantes

Ejemplo.-
	
	let implicitInteger = 70
	lest explicitDouble: Double = 70
	
	let label = “The width is ”
	let width = 94
	let widthLabel  = label + String(width)

	let appleSummmary = “I have \(apples) apples.”
	let fruitSummmary = “I have \(apples + apples2) apples.”

	let optionalInt: Int? = 9
	let actualInt: Int = optionalInt!

	// ! .- operador de desenvoltura 

	var possibleInt = Int(myString)
	print(possibleInt)

	var ratingList = [“Poor”,”Good”]
	ratingList[1] = “Ok”

	// Creates an empty array
	let emptyArray = [String]()

	// Flujo de control

	if number < 10{
		print(“The number is small”)	

	}else 
		print(“The number is big”)	


	for score in individualScores{
		// Code
	}

	var optionalName: String? = “Jhon Appleseed”
	var greeting = “Hello!”
	if let name = optionalName{
		greeting = “Hello, \(name)”
	}

	if let hello = optionalHello where hello.hasPrefix(“H”), let name = optionalName{
		greeting = “\(hello), \(name)”
	}

	switch variable{
		case “1”:
			let i = 1
		case “2”:
			let i = 2
		clase let x where x.hasSuffix(“H”):
			let i = “Hello”
		default:
			let i = -1
	}

	var i = 0
	for i in 0..<4{
		code
	}

	for _ in 0..<4{
		code
	}

	fun greet(name: String, day: String) -> String{
		return “Hello \(name), today is \(day)”
	}

	



